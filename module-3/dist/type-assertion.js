"use strict";
// let emni: any;
// emni = "Next Level web Development";
// // (emni as string).toUpperCase();
// <string>emni.toUpperCase();
// // console.log(emni);
// function kgToGram(param: string | number): string | number | undefined {
//   if (typeof param === "string") {
//     const value = parseFloat(param) * 1000;
//     return `${value} Gram`;
//   }
//   if (typeof param === "number") {
//     const value = param * 1000;
//     return `${value} Gram`;
//   }
// }
// const resultToBeNumber = kgToGram(5) as number;
// // const resultBeNumber = kgToGram("10") as string;
// const resultBeNumber = <string>kgToGram("10");
// console.log(resultToBeNumber);
// console.log(resultBeNumber);
// type CustomErrorType = {
//   message: string;
// };
// try {
// } catch (err) {
//   console.log((err as CustomErrorType).message);
// }
